<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="10dp"
    tools:context="com.example.bo.zxingdemo.LoginActivity"
    >
    <EditText
        android:id="@+id/phone"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="请输入手机号码"
        />
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        >
        <EditText
            android:id="@+id/check_code"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="2"
            android:hint="请输入验证码"
            />
        <com.coorchice.library.SuperTextView
            android:id="@+id/get_check_code"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:padding="5dp"
            android:text="获取验证码"
            android:textSize="20sp"
            app:corner="10dp"
            app:stroke_color="#00ff00"
            app:stroke_width="2dp"
            app:text_stroke="true"
            app:text_stroke_color="#ff55ff"
            app:text_stroke_width="1dp"
            />
    </LinearLayout>

    <TextView
        android:id="@+id/login"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="20dp"
        android:text="登录"
        android:textColor="@color/white"
        android:textSize="20sp"
        />

    <com.coorchice.library.SuperTextView
        android:id="@+id/test"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="300dp"
        android:background="#FF365E1F"
        android:padding="5dp"
        android:text="免登陆进入"
        android:textSize="10sp"
        app:corner="10dp"
        app:left_bottom_corner="true"
        app:stroke_width="2dp"
        app:text_stroke="true"
        app:text_stroke_color="#c9a454"
        app:text_stroke_width="1dp"
        />

</LinearLayout>
